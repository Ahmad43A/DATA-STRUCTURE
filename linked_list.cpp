#include <bits/stdc++.h>
using namespace std;

int main()
{
    //creating a node 
    typedef struct node
    {
        int number;
        struct node *next;

    }node;

    //initialize to null in case of garbage value exists
    node *list = NULL;

    node *n = new [node];
    printf("input elements in the list");








}
